[project]
name = "voting-rollback"
authors = ["Your Team"]
description = "A decentralized voting system with rollback capabilities on the Stacks blockchain"
telemetry = false
requirements = []
[contracts.voting-governance]
path = "contracts/voting-governance.clar"
depends_on = []

[contracts.voting-permissions]
path = "contracts/voting-permissions.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
